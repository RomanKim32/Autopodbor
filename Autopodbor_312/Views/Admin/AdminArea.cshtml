<header>

</header>
<body>
     @if(User.IsInRole("admin")) {
    <div class="btn-group">
  <button class="btn btn-secondary btn-lg dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
    Service's
  </button>
  <ul class="dropdown-menu">
      <li> <a class="btn btn-info" asp-controller="Admin" asp-action="CreateServices">Create Service</a></li>
      <li> <a class="btn btn-info" asp-controller="Admin" asp-action="IndexServices">Show All Service's</a></li>
  </ul>
</div>
     }
      @if (User.IsInRole("admin")) {
  <div class="btn-group">
  <button class="btn btn-secondary btn-lg dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
    Order's
  </button>
  <ul class="dropdown-menu">
      <li> <a class="btn btn-info"  asp-controller="Order"  asp-action="Index">Show All Order's</a></li>
  </ul>
</div>
      }
       @if(User.IsInRole("portfolioManager") ||User.IsInRole("admin")  ) {
  <div class="btn-group">
  <button class="btn btn-secondary btn-lg dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
   Portfolio's
  </button>
  <ul class="dropdown-menu">
      <li> <a class="btn btn-info"  asp-controller="Media"  asp-action="CreatePortfolio">Create Portfolio</a></li>
      <li> <a class="btn btn-info" asp-controller="Media" asp-action="IndexPortfolio">Show All Portfolio's</a></li>
  </ul>
</div>
       }
       @if(User.IsInRole("mediaManager") ||User.IsInRole("admin")  ) {
  <div class="btn-group">
  <button class="btn btn-secondary btn-lg dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
    News
  </button>
  <ul class="dropdown-menu">
      <li> <a class="btn btn-info" asp-controller="Media" asp-action="CreateNews">Create News</a></li>
      <li> <a class="btn btn-info" asp-controller="Media" asp-action="IndexNewss">Show All News</a></li>
  </ul>
</div>
       }

</body>
<footer>

</footer>

